- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''    
    ~/.bashrc: bashrc
    ~/.bashalias: bashalias

- shell:
  - [git submodule update --init --recursive, Installing submodules]
